# Testing Standards

- Lint on save (if configured) and zero console errors on demo path.
- Manual matrix: ping echo (normal & reduced motion), toast timing, timer resume, hotkeys, seeded reproducibility.
