# Accessibility Steering

- Keyboard-only path validated; controls documented.
- Toasts use `aria-live="polite"`; dismissable via Esc/Enter/Click.
- High-contrast toggle (`h`), Reduced-motion toggle (`r`).
- Contrast target ≥ WCAG AA in high-contrast mode.
