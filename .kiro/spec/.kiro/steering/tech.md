# Tech Steering

- Vanilla JS + HTML canvas; HTML5 Audio for SFX.
- State machine: idle → entering → active → complete → reset.
- Feature flags for DEV, reduced motion, high contrast.
- Deterministic seed via URL param `?seed=<int>` for QA reproducibility.
