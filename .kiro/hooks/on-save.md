# Hook Concept: on-save

On JS file save:
1) Run a lightweight lint.
2) Optionally insert a 🎃 tag comment on changed lines (demo-only).
3) Print summary to console.

(Implementation can be a pre-commit or small Node script; excluded here to keep the kernel portable.)
